using UnityEngine;

public class AstroidProperties : DestroyableObject  {
	//void FixedUpdate() {
		//if (gameObject.rigidbody2D.IsSleeping()) return;

		// Vector3 pos = Camera.main.WorldToViewportPoint(transform.position);


		// if (((pos.x > 1.0 && pos.x <= 1.3) || (pos.x > -0.3 && pos.x <= 0.0)) && ((pos.y > 1.0 && pos.y <= 1.3) || (pos.y > -0.3 && pos.y <= 0.0))) {
		// 	if (gameObject.rigidbody2D.velocity.x == 0 && gameObject.rigidbody2D.velocity.y == 0) {
		// 		Debug.Log(pos);
		// 		//gameObject.rigidbody2D.AddForce(new Vector2(Random.Range(0f, 30f), Random.Range(0f, 30f)));
		// 	}
		// }
	//}
}
